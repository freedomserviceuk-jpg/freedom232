# freedom232

Aurelia Suite Noir — a premium, black-glass hotel guest dashboard skin.

Open `index.html` directly in a browser, or serve the folder with any static file server. This is a front-end skin only: layout, styling, and interaction states (hover/active/focus/disabled) are complete, but data is static/placeholder — wire it up to real booking, messaging, and content sources separately.
